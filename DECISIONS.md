# 技术与产品决策

## 2026-08-15：MVP 初始化

### 状态

已采用

### 决定

采用 Next.js、TypeScript、Tailwind CSS 与 Supabase；首版服务单个地点，用户手动认猫，登录后动态直接发布并支持追溯治理。

### 原因

先验证社区参与和内容沉淀，避免在缺少标注数据时投入不可靠的自动识别。

### 关键约束

- 新猫申请生成临时档案，由工作人员确认、合并或驳回。
- 公开内容仅包含模糊区域；客户端重新编码图片以清除 EXIF。
- 征集使用固定字段；联系方式通过 RLS 仅向投稿者和工作人员开放。
- 未配置 Supabase 时保留演示模式，便于界面审阅，不作为生产数据源。

## 2026-08-15：采用 2023 图鉴与独立服务器后台

### 状态

已部署

### 决定

首页使用 2023 图鉴的薄荷绿纸张拼贴风格，录入 10 只可辨认猫咪；生产环境使用独立 Django Admin、SQLite 和服务器现有腾讯云 COS，运行于 `3180/3181`，由 Nginx 映射到 `/cat/`。

### 原因

工作人员需要可立即维护猫咪与时间线的后台，同时必须与服务器现有 Django、Node 和 Nginx 服务隔离。
