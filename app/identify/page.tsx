import Link from "next/link";
import { ArrowRight, HelpCircle, MapPin } from "lucide-react";
import { CatPhoto } from "@/components/CatPhoto";
const unknown=[{id:"u1",date:"8月14日",area:"体育馆东侧",pos:"91% center",note:"奶油色，尾巴较浅"},{id:"u2",date:"8月11日",area:"西门停车棚",pos:"67% center",note:"纯灰，金色眼睛"}];
export default function IdentifyPage(){return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6 sm:py-16"><p className="eyebrow">HELP IDENTIFY</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">这些猫咪，等你来认</h1><p className="mt-3 max-w-2xl text-ink/55">上传者暂时不知道它是谁。请根据花色和大致区域提供线索，工作人员会在确认后归入正确档案。</p><div className="mt-8 grid gap-5 sm:grid-cols-2">{unknown.map(item=><article className="card overflow-hidden p-0" key={item.id}><div className="relative aspect-[16/10]"><CatPhoto position={item.pos}/><span className="absolute left-3 top-3 rounded-full bg-amber-100/90 px-3 py-1 text-xs font-medium text-amber-900"><HelpCircle size={13} className="mr-1 inline"/>待认领</span></div><div className="p-5"><p className="flex items-center gap-1.5 text-xs text-ink/45"><MapPin size={13}/>{item.area} · {item.date}</p><p className="mt-3 font-medium">辨认线索：{item.note}</p><div className="mt-5 flex gap-2"><Link href="/cats" className="btn-secondary">从图鉴中选择</Link><Link href="/login" className="btn-primary">我认识它 <ArrowRight size={15}/></Link></div></div></article>)}</div></div>}
