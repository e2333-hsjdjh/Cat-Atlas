import { CalendarDays, FileText } from "lucide-react";
import { campaigns } from "@/lib/data";
import { CampaignForm } from "@/components/CampaignForm";
export default function CampaignsPage(){return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6 sm:py-16"><p className="eyebrow">OPEN CALL</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">征集与活动</h1><p className="mt-3 max-w-2xl text-ink/55">照片、文字和线索都会帮助我们更完整地认识这些邻居。联系方式只有工作人员能够查看。</p><div className="mt-10 space-y-8">{campaigns.map((campaign,i)=><section id={campaign.id} key={campaign.id} className="grid scroll-mt-24 overflow-hidden rounded-[2rem] border border-ink/10 bg-white/65 lg:grid-cols-[.9fr_1.1fr]"><div className={`${i===0?"bg-apricot":"bg-moss"} p-7 text-white sm:p-10`}><FileText size={32}/><p className="mt-8 text-xs text-white/65">{campaign.status==="open"?"正在征集":"已结束"}</p><h2 className="mt-2 text-3xl font-semibold">{campaign.title}</h2><p className="mt-5 leading-7 text-white/80">{campaign.summary}</p><div className="mt-8 flex flex-wrap gap-4 text-sm text-white/70"><span className="flex items-center gap-1.5"><CalendarDays size={15}/>截止 {campaign.deadline}</span><span>{campaign.submissions} 份投稿</span></div></div><div className="p-7 sm:p-10"><h3 className="mb-5 text-lg font-semibold">提交你的内容</h3><CampaignForm campaignId={campaign.id}/></div></section>)}</div></div>}
