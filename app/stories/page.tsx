import Link from "next/link";
import { ArrowRight, Quote } from "lucide-react";
import { cats, timeline } from "@/lib/data";
import { CatPhoto } from "@/components/CatPhoto";
export default function StoriesPage(){const stories=timeline.filter(t=>t.type==="故事");return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6 sm:py-16"><p className="eyebrow">CAT STORIES</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">关于它们的小故事</h1><p className="mt-3 text-ink/55">从一张照片、一段记忆开始，把共同生活的时间认真保存下来。</p><div className="mt-10">{stories.map((story)=>{const cat=cats.find(c=>c.slug===story.catSlug)!;return <article key={story.id} className="grid overflow-hidden rounded-[2rem] bg-ink text-cream lg:grid-cols-2"><div className="relative min-h-80"><CatPhoto position={cat.imagePosition}/></div><div className="flex flex-col justify-center p-7 sm:p-12"><Quote className="text-apricot"/><p className="mt-5 text-xs text-cream/50">{story.date} · {cat.name}</p><h2 className="mt-3 text-3xl font-semibold">{story.title}</h2><p className="mt-5 leading-8 text-cream/70">{story.body}</p><Link className="mt-7 flex items-center gap-2 text-sm font-medium" href={`/cats/${cat.slug}`}>查看{cat.name}的档案 <ArrowRight size={16}/></Link></div></article>})}</div><div className="mt-8 card text-center"><h2 className="text-xl font-semibold">你的故事，也值得被留下</h2><p className="mt-2 text-sm text-ink/55">参与当前征集，优秀投稿会在与你确认后收录为精选故事。</p><Link href="/campaigns" className="btn-primary mt-5">查看征集活动</Link></div></div>}
