# 项目协作规则

## 项目目标

让校园或社区成员安全、方便地认识猫咪、记录动态并参与内容共建。

## 工作方式

- 修改前阅读 `SPEC.md` 与相关代码，保留用户已有修改。
- 优先复用现有组件，不修改无关内容，不把密钥写入仓库。
- 位置只保存和展示用户主动填写的模糊区域；不得恢复或保存图片 EXIF 坐标。
- 权限判断必须在 Django 服务端完成，不能仅依赖隐藏前端按钮。
- 修改后运行 `npm run typecheck`、`npm test`、`npm run build` 和 `python3 backend/manage.py check`。

## 代码与界面约定

- 用户可见文本使用简体中文，代码与数据库字段使用英文。
- 移动端优先，交互控件需要键盘焦点、可读标签和触摸尺寸。
- 高频交互不添加冗长动效，并尊重 `prefers-reduced-motion`。

## 完成标准

- 行为符合 `SPEC.md`；相关检查通过；需求、任务和变更文档保持一致。
